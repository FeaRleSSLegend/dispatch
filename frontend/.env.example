# Production only: the public URL of the Dispatch API.
VITE_API_URL=https://dispatch-backend-07jy.onrender.com

# Development only: Vite forwards same-origin /api/* to this host.
# Commented out by default; uncomment to hit a remote backend in dev.
# DISPATCH_API_PROXY_TARGET=http://127.0.0.1:8000
